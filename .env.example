# Optional override for the inference Worker URL used at build time.
# Defaults to the staging Worker when unset.
# For production builds (Phase 5):
#   WORKER_URL=https://echoaware-api.<subdomain>.workers.dev/v1/escape-queries
WORKER_URL=
