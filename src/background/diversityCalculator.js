// Unweighted Simpson's Diversity Index: D = 1 - Σ(nᵢ/N)²
// @param {Array<{clusterId: number, size: number}>} clusterSizes
// @returns {number} D in [0, 1]
export function calculateSimpsonsDiversity(clusterSizes) { throw new Error('Not implemented'); }
